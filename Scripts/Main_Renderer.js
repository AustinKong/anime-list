const anime = ["overflow", "kaguyasama"];

anime.forEach(element => {
    
});

const mainGallery = document.getElementById('mainGallery');
function CreatePanel(){
    const panel = document.createElement("img");
    //panel.src = APIGet(11741);
    mainGallery.create
}